[project]
name = "claude-agent-bridge"
version = "0.1.0"
description = "MCP bridge for using local Claude CLI as a bounded reviewer and analysis delegate for Codex."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }

[tool.pytest.ini_options]
testpaths = ["tests"]
